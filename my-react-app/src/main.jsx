import React from 'react';
import ReactDOM from 'react-dom/client';
import BuildnBoostLanding from './App';
import './index.css'; // Make sure this path is correct
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BuildnBoostLanding />
  </React.StrictMode>
);